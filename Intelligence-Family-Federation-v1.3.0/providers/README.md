# Providers

Providers are external Intelligence/model ecosystems.

Provider adapters belong here when an actual integration is implemented. Documentation-only Family members should not be represented as executable adapters.

Provider membership does not imply ownership, endorsement, or unrestricted access.
