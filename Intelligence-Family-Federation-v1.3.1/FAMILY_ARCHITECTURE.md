# Family Architecture

```text
Human Authority
      |
Intelligence Family
      |
Providers -> Capabilities -> Skills -> Agents
      |                         |
      +------ Governance --------+
                    |
                 SENTINEL
                    |
             Human Review
                    |
                 Action
```

The architecture separates identity, capability, execution, and authorization so that adding an agent does not automatically add authority.
