# Contract Versioning

Federation contracts use explicit semantic versions.

- **MAJOR** — incompatible contract change
- **MINOR** — backward-compatible capability addition
- **PATCH** — backward-compatible clarification or correction

Contract changes require documentation and review. A contract version does not imply that a provider's underlying model has the same version.
